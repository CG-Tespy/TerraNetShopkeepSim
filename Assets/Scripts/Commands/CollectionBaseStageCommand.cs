using UnityEngine;
using Fungus;

public class CollectionBaseStageCommand : CollectionBaseCommand
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
